@using DomainBookShop.Entities
@model IEnumerable<Book>

@{
    ViewBag.Title = "List";
}

<h2>List</h2>

@foreach (Book book in Model)
{
    <div>
        <h3>@book.Name - @book.Author</h3>
        <p>@book.Description</p>
        <h4>@book.Price.ToString("# грн")</h4>
    </div>
    <hr />
}